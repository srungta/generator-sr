import { html, when } from "@microsoft/fast-element";
import { <%= componentClassName %> } from "./<%= componentClassName %>";

export const <%= componentClassName %>Template = html<<%= componentClassName %>>`
`;
